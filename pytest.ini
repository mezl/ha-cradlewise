[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    hilt: Hardware-in-the-Loop tests (require real crib)
    functional: Functional tests against live HA REST API
    unit: Pure unit tests (no network)
addopts = -v --tb=short
